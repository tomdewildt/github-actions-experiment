[project]
name = "github-actions-experiment"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.3",
    "sqlalchemy>=2.0.50",
    "uvicorn>=0.48.0",
]

[project.optional-dependencies]
dev = [
    "black>=26.5.1",
    "mypy>=2.1.0",
]
