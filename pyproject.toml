[project]
name = "github-actions-experiment"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.3",
    "sqlalchemy>=2.0.0",
    "uvicorn>=0.22.0",
]

[project.optional-dependencies]
dev = [
    "black>=23.3.0",
    "mypy>=1.4.0",
]
